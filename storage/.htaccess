# Belt and braces. Nothing under /storage is ever served directly — every file
# is delivered by download.php after an authorisation check.
Require all denied
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
